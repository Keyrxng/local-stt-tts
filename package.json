{
  "name": "local-stt-tts",
  "description": "Local speech-to-text and text-to-speech with Whisper and Orpheus",
  "module": "./bundle.js",
  "types": "./dist/src/main.d.ts",
  "type": "module",
  "author": {
    "name": "Keyrxng",
    "email": "keyrxng@proton.me"
  },
  "scripts": {
    "build": "bun run build:types && bun run build:bundle",
    "build:bundle": "bun build --target=node --outfile=bundle.js ./src/main.ts",
    "build:types": "tsc",
    "t": "bun build --target=node --outfile=bundle.js ./t.ts"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.23",
    "@picovoice/pvrecorder-node": "^1.2.8",
    "ai": "^5.0.28",
    "dotenv": "^17.2.1",
    "ollama": "^0.5.17",
    "wavefile": "^11.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/glob": "^9.0.0",
    "@types/node": "^24.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
